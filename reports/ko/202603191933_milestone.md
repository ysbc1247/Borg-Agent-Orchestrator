# 마일스톤 체크포인트

타임스탬프: 2026-03-19 19:33 KST

## 현재 저장소 상태

- 지점 : `main`
- 원격 동기화: `origin/main`
- 작업 트리: 체크포인트 시간에 정리

## 이전 마일스톤 이후 완료된 작업

- `~/Documents/borg_processed` 아래 생성된 parquet 및 아티팩트 디렉터리 옆에 로컬 스키마 설명 파일을 추가했습니다.
- 원시 처리된 parquet 유형, 결합된 데이터 세트, 예측 데이터 세트, 표준 공통 예측 데이터 세트 및 기준 아티팩트 디렉토리에 대한 문서화된 의미.
- `~/Documents/borg_processed` 아래의 새 parquet 또는 아티팩트 디렉터리 옆에 스키마 또는 아티팩트 설명 파일을 생성해야 하는 내구성 있는 저장소 규칙이 추가되었습니다.
- 향후 Codex 세션을 위해 핸드오프 파일에 동일한 parquet-schema-note 규칙을 기록했습니다.

## 현재 운영 규칙

- 관심사와 파일 클래스별로 커밋을 공격적으로 분할합니다.
- `reports/` 형식으로 KST 타임스탬프가 앞에 붙은 파일 이름을 `YYYYMMDDHHMM_*` 형식으로 사용하세요.
- 세션을 종료하기 전에 추적된 메모리 파일에 현재 상태를 유지하라는 명령으로 `milestone`을 처리합니다.
- `~/Documents/borg_processed` 아래에 새로운 파켓 유형이나 아티팩트 디렉터리가 생성되면 동일한 디렉터리에 스키마나 아티팩트 설명 파일을 배치합니다.

## 현재 다음 단계

- 예제 로컬 클라우드 매핑을 실제 원격 측정 매핑으로 바꾸고 첫 번째 표준 로컬 클라우드 데이터 세트를 생성합니다.
- 그 후, 표준 스키마 위에 더 강력한 예측 기준을 추가합니다.