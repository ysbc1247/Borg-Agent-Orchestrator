# 2026-03-27 16:32 KST 마일스톤

## Summary

이 이정표는 원래 테스트 예측자 워크플로에서 격리된 Advanced XGBoost 워크플로로의 전환을 포착합니다.

주요 Result:

- 고급 데이터 및 모델 workspace은 `~/Documents/borg_xgboost_workspace` 아래에 격리되어 있습니다.
- 고급 원시 다운로더는 이제 `g`을 통해 `b` 클러스터에 대한 고정 일치 샤드 계획을 지원합니다.
- 고급 소스 코드는 `src/advanced_xgboost`로 구분됩니다.
- Advanced feature 엔지니어링에는 이제 더 풍부한 운영 및 시간 기능이 포함됩니다.
- Advanced XGBoost에 대한 누락 데이터 처리는 레이블이 유효한 행을 유지하고 숫자 Null을 보존하며 명시적인 누락 플래그를 추가합니다.
- `5m`, `15m`, `30m`, `45m`, `60m`에 대한 다중 수평 대상이 준비됩니다.
- `.venv`을 통해 Repo-local 런타임 설정이 준비되었으며 `polars`, `xgboost` 및 macOS `libomp`이 확인되었습니다.

## 현재 고급 다운로드 스냅샷

마일스톤 시간에 `~/Documents/borg_xgboost_workspace/raw`에서 관찰됨:

- `events`: `46` 파일
- `usage`: `41` 파일
- `machines`: `3` 파일

대표 주:

- `b`에는 머신 샤드 `000000000000` 및 이벤트/사용 샤드 `000000000000` ~ `000000000014`이 있습니다.
- `c`에는 머신 샤드 `000000000000`이 있고 최소한 초기 이벤트/사용 샤드가 존재합니다.
- `d`에는 머신 샤드 `000000000000`이 있습니다.
- 고급 다운로드가 아직 진행 중이며 완료된 고정 샤드 세트에 대한 다운스트림 처리가 아직 실행되지 않았습니다.

## Advanced pipeline 준비

준비된 스크립트:

- `./scripts/setup_advanced_runtime.sh`
- `./scripts/run_advanced_download.sh`
- `./scripts/run_advanced_flatten.sh`
- `./scripts/run_advanced_join.sh`
- `./scripts/run_advanced_feature_build.sh`
- `./scripts/run_advanced_train.sh`
- `./scripts/run_advanced_xgboost_pipeline.sh`

확인된 런타임:

- `polars 1.39.3`
- `xgboost 2.1.4`

## 다음 실행 순서

원시 다운로드가 완료되면 다음을 실행합니다.

```bash
./scripts/run_advanced_flatten.sh
./scripts/run_advanced_join.sh
./scripts/run_advanced_feature_build.sh
./scripts/run_advanced_train.sh
```

또는 전체 체인을 실행합니다.

```bash
./scripts/run_advanced_xgboost_pipeline.sh
```

## 중요 참고사항

- 원래 기준선/테스트 파이프라인은 `~/Documents/borg_data` 및 `~/Documents/borg_processed` 아래에 그대로 유지됩니다.
- 고급 트랙은 `~/Documents/borg_xgboost_workspace`만 계속 사용해야 합니다.
- 고급 트레이너는 별도의 조인된 데이터 세트 없이 동일한 기능 parquet에서 목표 horizon당 하나의 모델을 학습할 준비가 되어 있습니다.