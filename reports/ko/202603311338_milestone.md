# 마일스톤 체크포인트

타임스탬프: `2026-03-31 13:38 KST`

## 현재 live process

- 활성 래퍼:
  - `./scripts/run_advanced_train_resumable.sh`
- 활성 트레이너:
  - `scripts/train_advanced_xgboost.py`
- 현재 튜닝된 모델명 :
  - `xgboost_failure_risk_tuned_v1`
- 현재 tuned retrain 로그:
  - `~/Documents/borg_xgboost_workspace/runtime/logs/20260331111307_advanced_train_resumable.log`

## 체크포인트에서 관찰된 런타임 상태

- tuned retrain은 아직 첫 번째 단계에 있습니다.
  - `target_failure_5m`
- 아직 tuned 모델 아티팩트가 방출되지 않았습니다.
  - `~/Documents/borg_xgboost_workspace/models/xgboost/xgboost_failure_risk_tuned_v1_*`
- 라이브 Python 트레이너는 여전히 많은 CPU를 소비하므로 이 체크포인트에서는 죽은 프로세스가 아닙니다.

## 이 마일스톤 이전에 완료됨

- 고급 병합 및 결합 수정
- `b`에서 `g`까지 클러스터에 대한 결합된 데이터 세트가 완료되었습니다.
- `b`에서 `g`까지 클러스터에 대한 Advanced feature parquet 생성 완료
- Horizon `5m`, `15m`, `30m`, `45m`, `60m`에 대한 기본 생산 교육 완료
- 생성된 이중 언어 평가 보고서:
  - `reports/202603311050_advanced_training_evaluation_en.md`
  - `reports/202603311050_advanced_training_evaluation_ko.md`
- 파일럿 튜닝 스윕을 실행하고 `regularized_balanced`을 현재 우승자로 선택했습니다.

## 현재 튜닝 우승자

- 후보자 :
  - `regularized_balanced`
- 매개변수:
  - `n_estimators=1600`
  - `max_depth=6`
  - `learning_rate=0.03`
  - `subsample=0.9`
  - `colsample_bytree=0.7`
  - `min_child_weight=8`
  - `reg_alpha=0.2`
  - `reg_lambda=2.0`
  - `early_stopping_rounds=80`

## 이력서 안내

새로운 Codex 컨텍스트에서 재개하는 경우:

1. `20260331111307_advanced_train_resumable.log`에 연결된 프로세스가 아직 살아 있는지 확인합니다.
2. 완료되면 `xgboost_failure_risk_tuned_v1`을 기준 프로덕션 모델과 비교합니다.
3. `5m` 아티팩트를 생성하지 않고 시간이 너무 오래 걸리는 것 같으면 더 작은 한도나 추정기 예산으로 tuned retrain을 다시 시작하세요.
4. tuned 모델이 성공하면 tuned 아티팩트를 사용하여 이중 언어 보고서를 재생성하고 새로운 최종 Summary을 유지합니다.